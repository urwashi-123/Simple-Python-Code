from collections import Counter
num=[1,2,3,3]
freq=Counter(num)
print(freq)


# A counter is a dictionary subclass that maps elements to their counts.It's like a dictionary where the keys are the elements and the values are the counts.